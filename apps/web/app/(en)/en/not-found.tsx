import { NotFoundPage } from "@/components/not-found-page";

// Mirrors (sl)/not-found.tsx under the English root layout.
export default function NotFound() {
  return <NotFoundPage locale="en" />;
}
